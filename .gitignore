# Logs
logs
*.log
npm-debug.log*

# Dependencies
node_modules
dist
dist-ssr

# Editor
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Tauri build output
src-tauri/target/
src-tauri/gen/schemas/
src-tauri/WixTools/

# Local env
.env
.env.*
!.env.example

# Data raw descargada de APIs (no commit — usar scripts para regenerar)
# IMPORTANTE: solo el /data en raíz, no afectar src/lib/data/
/data/

# Tooling local de dev (no es código del juego — específico de la máquina)
/JUGAR-MRO.bat
/serve-build.cjs

# Temporales de render / snapshots sueltos
/_*.png
/builds/*-snapshot.html
