# sim/

Motor de simulación puro. Cero DOM, cero Svelte stores aquí — solo lógica.

Submódulos previstos en Bloque C: `time.ts` (tick), `contracts.ts`, `airplanes.ts`, `workorders.ts` (state machine 5 fases), `mechanics.ts` (asignación), `economy.ts` (cierre semanal).
