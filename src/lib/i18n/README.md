# i18n/

Locales ES (default) + EN portados del legacy. Función `t(key, params?)` + store `currentLocale`.

Convención: claves anidadas (`hud.balance`, `wo.phase.inspection`). Emojis Unicode embebidos en valores según legacy.
