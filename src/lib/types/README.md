# types/

Tipos TypeScript del dominio: `WorkOrderTemplate`, `WorkOrderInstance`, `Mechanic`, `Airplane`, `Contract`, `Airline`, `Stand`, etc.

Convención: un fichero por agregado. Schemas runtime (validación JSON entrante) van junto al tipo como `*.schema.ts`.
