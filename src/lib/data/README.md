# data/

Datos estáticos del juego: `workorders.json` (100 WOs portadas del legacy), `airlines.json` (4 aerolíneas), `balance.json` (constantes de tuneo).

NO contiene mecánicos/aviones runtime — eso vive en stores. Estos JSON son solo lo que se carga al new game.
