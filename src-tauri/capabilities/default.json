{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities por defecto para la ventana principal del MVP.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:app:allow-version",
    "sql:default",
    "sql:allow-execute",
    "sql:allow-load",
    "sql:allow-select"
  ]
}
